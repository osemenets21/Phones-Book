import { SortLastNamePipe } from './sort-last-name.pipe';

describe('SortLastNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortLastNamePipe();
    expect(pipe).toBeTruthy();
  });
});
